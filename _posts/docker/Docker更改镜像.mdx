---
date: '2021-01-08'
title: '基于Docker镜像的更新'
tags: [docker]
---

1. 停止镜像

```bash
docker stop <ID or name>
```

2. 移除镜像

```bash
docker rm <ID or name>
```

3. 拉取最新镜像

```bash
docker pull <image name>
```

4. 启动镜像

```bash
docker run <image name> ...options
```

## 更多
